module.exports = (priorData, data) =>
    new Promise((resolve, reject) => {
        console.log('Need to implement "MERGE"!');
        try {
            resolve(data);
        } catch (err) {
            reject(err);
        }
    });
